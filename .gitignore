.env

# Binaries extracted from container images at build time (see
# .build/fetch-binaries.sh). Not checked in.
/binaries/

# Debian build artifacts.
/debian/mlab-node/
/debian/.debhelper/
/debian/files
/debian/*.debhelper
/debian/*.debhelper.log
/debian/*.substvars
/debian/debhelper-build-stamp
*.deb
*.buildinfo
*.changes