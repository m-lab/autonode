.env

# Binaries built from source at build time (see
# .build/build-binaries.sh). Not checked in.
/binaries/

# Debian build artifacts.
/debian/mlab-node/
/debian/.debhelper/
/debian/files
/debian/*.debhelper
/debian/*.debhelper.log
/debian/*.substvars
/debian/debhelper-build-stamp
*.deb
*.buildinfo
*.changes