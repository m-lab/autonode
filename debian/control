Source: mlab-node
Section: net
Priority: optional
Maintainer: Measurement Lab <support@measurementlab.net>
Build-Depends:
 debhelper-compat (= 13),
 file,
 golang-go (>= 2:1.21~),
 git,
 ca-certificates,
 gcc,
 make,
 libc6-dev,
Standards-Version: 4.6.2
Homepage: https://github.com/m-lab/autonode
Rules-Requires-Root: no

Package: mlab-node
Architecture: amd64 arm64
Depends:
 ${shlibs:Depends},
 ${misc:Depends},
 curl,
 kmod,
Description: M-Lab node (autonode) measurement stack
 Native systemd packaging of the M-Lab "autonode" Network Diagnostic Tool
 (NDT) measurement platform, replacing the former Docker Compose deployment.
 .
 It runs ndt-server and its sidecar services - node registration, heartbeat,
 uuid-annotator, jostler, traceroute-caller and a Prometheus node exporter.
